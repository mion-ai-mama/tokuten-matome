# 特典まとめページ（tokuten-matome）

> 設計の共通原則（基本原則・資産価値の原則・自律解決の原則）は `~/.claude/CLAUDE.md` に従う。

Instagramで配布してきた全特典（GitHubページ / note記事 / プロンプト単体）を1枚に
集約する静的サイト。要件は `docs/requirements.md`、進捗は `docs/SCOPE_PROGRESS.md`。

## プロジェクト設定

技術スタック:
  frontend: HTML5 / CSS3 / Vanilla JavaScript（ビルド工程なし・フレームワーク不使用）
  backend: なし
  database: なし
  hosting: GitHub Pages

リポジトリ: https://github.com/mion-ai-mama/tokuten-matome
公開URL: https://mion-ai-mama.github.io/tokuten-matome/

ビルド・サーバーが不要な完全な静的サイト。`index.html` をブラウザで直接開くか、
`python3 -m http.server` で確認できる。ポート番号のランダム生成・バックエンドポートの
割り当ては不要。

## 発信者情報（ページに出す名乗り）

- 表示名: みおん｜在宅ワークの3児ママ
- Instagram: `@mion.ai.mama` → https://www.instagram.com/mion.ai.mama/
- GitHubアカウント: `mion-ai-mama`（発信用。本名ベースの `satomi-saiga` は使わない）

## 環境変数

このプロジェクトは環境変数を使用しない（APIキー・DB接続情報が一切不要な静的サイトの
ため）。`.env` 系ファイルは作成しない。

## コンテンツの単一の源

**`js/presents.js` の `PRESENTS` 配列が、掲載特典の唯一の真実の源。**
ここに1件追記すれば、カード・カテゴリー別件数・総件数がすべて自動更新される。
`index.html` を手で編集して特典を足すことは禁止（二重管理になり必ずズレる）。

### 新しい特典を追加するときの手順

ユーザーから「まとめに追加して」と言われたら、以下を実行する。

1. 掲載する特典のURLへ実際にアクセスし、到達すること（HTTP 200）を確認する
2. `js/presents.js` の `PRESENTS` 配列の、**その特典の種別グループの先頭**に1件追記する
   - ⚠️ 配列全体の先頭ではない。note特典を足すなら note グループの先頭に入る
3. `number` は **いま使われている最大の番号 + 1** を付ける（`"27"` `"28"` … の形式）
   - ⚠️ 既存の番号は振り直さない。番号はその特典に固定のIDとして扱う
   - 古い特典ほど小さく、新しい特典ほど大きい番号になる
4. サムネイルを用意する
   - 特典ページに固有のヒーロー画像がある → `assets/thumbs/` に取り込んで使う
   - 無い → `node tools/gen-thumb.js <id>` でデザインを自動生成する
5. `node tools/stamp-images.js` を実行して画像の版番号を更新する
   - これを忘れると、画像を差し替えてもブラウザが古い絵を最大10分表示し続ける
6. `node tools/validate.js` で検査を通す
7. ブラウザで表示を確認する（カードが出る・絞り込みに乗る・リンクが開く）
8. コミットしてpushする
9. **公開URLで実際に反映を確認してから完了を報告する**（手元で直っていても、
   pushを忘れていれば公開ページは変わらない）

### PRESENTS の1件の形

| フィールド | 必須 | 内容 |
|-----------|------|------|
| `id` | ✅ | 一意の識別子（kebab-case） |
| `number` | ✅ | 通し番号。古いものほど小さい。新規は最大+1で固定 |
| `type` | ✅ | `"page"` / `"note"` / `"prompt"` |
| `category` | ✅ | 下記3種のいずれか |
| `title` | ✅ | カード見出し（`<br>` 可） |
| `plainTitle` | ✅ | 検索用のタグなしタイトル |
| `description` | ✅ | 説明文（60〜90字目安） |
| `tags` | ✅ | タグ3つ程度 |
| `url` | △ | `page` / `note` のとき必須 |
| `prompt` | △ | `prompt` のとき必須（コピー対象の本文） |
| `image` | ✅ | サムネイルのパス（末尾の `?v=` は stamp-images.js が自動で付ける） |

### 表示の決まりごと

- **並び順 = 種別グループ順 → 各グループ内で新着順**

  ```
  ① 🎁 page（特典ページ）  … 新着順   ← 一番上
  ② 📝 note               … 新着順
  ③ 📋 prompt（プロンプト）           ← 一番下
  ```

  自分のページを上に置き、外部へ離脱するnoteを下げるため。
  配列の並び順がそのまま表示順。並べ替えUI・ピン留めは作らない

- **番号は並び順とは別の軸**。古い特典ほど小さく、新しい特典ほど大きい。
  そのため下にあるnote・プロンプトほど小さい番号になる
  （初期26件は、上から下へ 26 → 01 と減っていく）
- `type: "note"` のカードには「**こちらの特典は外部サイト note に遷移します**」の注釈を
  常時表示する。`page` / `prompt` には出さない
- `title`（カード見出し）は長い元題を**短く言い換えてよい**。
  ただし `plainTitle` には**元のタイトル全文**を必ず残す（検索性を落とさないため）

### カテゴリーは3つだけ（増やさない）

- `ChatGPT・AI活用`
- `画像・動画づくり`
- `AIで副業・在宅ワーク`

**テーマ軸で統一している。**「プロンプト集」「10選」「保存版」のような*形式*は
カテゴリーではなく `tags` で表現する。軸を混ぜると1件が複数カテゴリーに該当し、
絞り込みが破綻する。

## 命名規則

- ファイル: kebab-case（例: `gen-thumb.js`）
- JavaScript変数・関数: camelCase / 定数配列: `PRESENTS`（大文字で固定）

## コード品質

- 関数: 100行以下 / ファイル: 700行以下 / 複雑度: 10以下 / 行長: 120文字
- ビルド工程を持たないためLintツールは導入しない（最小性）。基準は遵守で運用する。

## セキュリティ（静的サイトでも必ず守る）

- **DOMへの挿入は必ずエスケープする。** `PRESENTS` の値・検索語を `innerHTML` に
  生のまま入れない（XSS）
- 外部リンクの `target="_blank"` には必ず `rel="noopener noreferrer"` を付ける
- 個人情報を取得・保存しない。フォーム・Cookie・トラッキングを置かない
- APIキー・認証情報を持ち込まない

## 開発ルール

### サーバー起動
- ローカル確認時は `python3 -m http.server <port>` を1つだけ起動する
- 別ポートでの重複起動は避ける

### リンクの健全性
- 特典を追加・変更した際は、掲載URLに実際にアクセスして到達を確認する（実証性）
- リンク切れを見つけたら放置せず、該当カードを直すか外す

### ドキュメント管理
許可されたドキュメントのみ作成可能:
- `docs/requirements.md`（要件定義）
- `docs/SCOPE_PROGRESS.md`（進捗管理）
- `README.md`（新特典の追加方法）
上記以外のドキュメント作成はユーザー許諾が必要。

### デプロイ
- デプロイはユーザーの明示的な承認を得てから実行する
- 公開はGitHub Pages（Enforce HTTPS を有効にする）
